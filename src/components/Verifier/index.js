import React, { Component } from "react";
import { connect } from "react-redux";

class Verifer extends Component {
  render() {
    return <h1>Verifer</h1>;
  }
}

export default connect(null, null)(Verifer);
